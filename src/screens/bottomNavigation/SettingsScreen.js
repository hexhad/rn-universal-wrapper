import { SafeAreaView, Text, View } from "react-native";
import "react-native-gesture-handler";
export default () => {
  return (
    <SafeAreaView>
      <Text>Settings</Text>
    </SafeAreaView>
  );
};
